package lineart.model;

/**
 * The position of an object relative to another object, looking in a clockwise
 * manner: left of an object, right of an object or hitting an object
 * @author noisedriver
 */
public enum Position {
    RIGHT, LEFT, HIT
}
